<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.khawaja_maintenance" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Appbar"
        tools:replace="android:icon" >
        <activity android:name=".Order_Generate" />
        <activity android:name=".Splash_ref2" />
        <activity
            android:name=".Fixit_Splash"
            android:theme="@style/AppTheme" />
        <activity android:name=".splash_dealing_two" />
        <activity android:name=".splash_dealing" />
        <activity android:name=".splash_referrals" />
        <activity android:name=".User_information" />
        <activity
            android:name=".Home_Screen"
            android:theme="@style/AppTheme" />
        <activity android:name=".SignUp" />
        <activity android:name=".Login" />
        <activity
            android:name=".paper_on_boarding"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>